language: java
jdk:
  - openjdk11
install:
  - true
before_install:
  - chmod +x gradlew
  - chmod +x gradle/wrapper/gradle-wrapper.jar
  - echo -e 'user = u\npassword = p' > local.properties
script:
  - ./gradlew test build